## Packages
(none needed)

## Notes
Tailwind is installed but will be largely bypassed in favor of plain CSS variables and semantic classes as requested.
API endpoint for contact form: POST /api/contact
