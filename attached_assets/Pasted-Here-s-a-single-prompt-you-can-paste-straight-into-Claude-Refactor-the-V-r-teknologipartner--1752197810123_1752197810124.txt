Here’s a single prompt you can paste straight into Claude:

````
Refactor the “Vår teknologipartner” section of my page to use a single large “featured” card for GlobalConnect at the top, with all other items displayed below as selectable grid cards. 

1. In index.html, replace your existing `<div class="hc-cards">` block under the “Vår teknologipartner” heading with:

```html
<!-- FEATURED CARD -->
<div class="hc-featured-card">
  <img src="gc-logo-hor-white.png" alt="GlobalConnect Logo" />
  <p>Norges største fiberleverandør med robust infrastruktur og 99,9% oppetid</p>
</div>

<!-- SELECTABLE GRID -->
<div class="hc-cards hc-cards--select">
  <div class="glass-card">Fiber til bedrift</div>
  <div class="glass-card">24/7 NOC-support</div>
  <div class="glass-card">Redundante forbindelser</div>
  <div class="glass-card">Personlig kontakt – vi følger deg hele veien</div>
  <div class="glass-card">Rask installasjon – vi kjenner montørene som gjør jobben</div>
  <div class="glass-card">14 års erfaring med GlobalConnect i Trondheim</div>
  <div class="glass-card">Norges største fibernett – levert lokalt</div>
  <div class="glass-card">4G-backup og 24/7 driftsovervåking inkludert</div>
  <div class="glass-card">Ingen overraskelser – du får alt forklart før vi setter i gang</div>
  <div class="glass-card">Vi er fysisk til stede ved installasjon – kostnadsfritt</div>
</div>
````

2. In style.css, append:

```css
.hc-featured-card {
  max-width: 600px;
  margin: 0 auto 2rem;
  padding: 2.5rem;
  background: var(--gradient-glass);
  border: 2px solid var(--gradient-border);
  border-radius: var(--radius-2xl);
  box-shadow: var(--shadow-large);
  text-align: center;
}
.hc-featured-card img {
  height: 80px;
  object-fit: contain;
  margin-bottom: 1rem;
}
.hc-featured-card p {
  font-size: 1.125rem;
  color: var(--text-secondary);
  margin: 0;
}

.hc-cards--select {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px,1fr));
  gap: 1rem;
  margin-bottom: 2rem;
}
.hc-cards--select .glass-card {
  cursor: pointer;
  transition: transform 0.2s, box-shadow 0.2s;
}
.hc-cards--select .glass-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-glow);
}
```

Apply these changes so the GlobalConnect box renders large and centered at the top, with the remaining benefits below in a neat selectable grid. \`\`\`
